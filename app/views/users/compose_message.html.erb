
<%=form_tag({:controller => "users", :action => "send_message"}) do%>
	<%= label_tag(:text, "Message Body. Messages over 160 characters will not be sent")%>
	<%= text_area_tag(:text) %>
	<%= submit_tag("Text!") %>
<% end %>